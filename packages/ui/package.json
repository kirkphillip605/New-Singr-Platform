{
  "name": "@singr/ui",
  "version": "0.0.1",
  "private": true,
  "description": "Shared glass UI design system — React components and CSS custom properties",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./styles": "./src/styles/index.css"
  },
  "scripts": {
    "build": "echo 'ui package uses live source — no build step'",
    "lint": "echo 'No lint yet'",
    "type-check": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "react": "^19.0.0"
  },
  "devDependencies": {
    "@singr/config": "workspace:*",
    "@types/react": "^19.0.0",
    "typescript": "^5.8.0"
  }
}
